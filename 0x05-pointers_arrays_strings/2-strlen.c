#include "main.h"
#include <stdio.h>

/**
 * strlen -  returns
 * length of a string
 *  *@s: integer entered
 *Return: void
 */
int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; ++i)
{
}
return (i);
}
